import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
     searchInput: {
    marginHorizontal: 16,
    marginVertical: 12,
    borderWidth: 1,
    borderColor: '#ccc',
    borderRadius: 8,
    padding: 10
  }

})